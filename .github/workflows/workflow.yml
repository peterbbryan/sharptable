# basic linting checks on master merge

name: workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:

  build:
  
    runs-on: ubuntu-latest
        
    steps:
    - name: MyPy check.
      run: |
         ls ${{ github.workspace }}
